//join
const array = ['ahmed', 'moe', 1, 2];
console.log(array.join(',')) //join thme into 1 string| ',' between

//split
const message = 'this is my first message';
const messageSplit= (message.split(' ')); // split only for String | use space to split them.

console.log(messageSplit.join('-')) //| join only works for Array(messageSplit)
